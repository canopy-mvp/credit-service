# Credit Service Context

TypeScript + Fastify backend. Prisma ORM.

Important: facility amounts are always in cents (integer). Never use floats for money.

Auth: JWT tokens validated in preHandler middleware.
